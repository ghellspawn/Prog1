#include <stdio.h>

void ler_arrayj(int x, int arrayj[], int i)
{
    if(i == x)
    {
        return;
    }
    else
    {
        scanf("%d")
    }
    
}

int main()
{
    int p, x;
    scanf("%d %d", &p, &x);
    int arrayj[x], arrayc[x];

    ler_arrayj(x, arrayj, 0);

    return 0;
}

// p = Indicando a quantidade necessária de pontos para vencer;
// x = Indicando o tamanho dos array da pontuação;

